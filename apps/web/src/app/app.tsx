import React from "react";

import { Layout } from "components/Layout/Layout";
import { Week } from "features/week";

export const App = () => {
  // todo: Add routing
  return (
    <Layout>
      <Week />
    </Layout>
  );
};

export default App;

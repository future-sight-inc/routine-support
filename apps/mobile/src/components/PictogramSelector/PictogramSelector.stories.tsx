import { storiesOf } from "@storybook/react-native";

import { PictogramSelector } from "./PictogramSelector";

storiesOf("Components", module).add("PictogramSelector", () => {
  return <PictogramSelector />;
});
